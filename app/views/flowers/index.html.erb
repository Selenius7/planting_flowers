<div class="page-header">
  <h1>Цветы</h1>
</div>
<%=	 render 'flowers_grid' %>

<%= link_to t('.new', :default => t("helpers.links.new")),
            new_flower_path,
            :class => 'btn btn-primary' %>
<button class="wg-external-csv-export-button btn btn-primary" data-grid-name="flowers">Экспорт в CSV</button>

