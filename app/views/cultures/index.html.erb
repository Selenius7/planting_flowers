<div class="page-header">
  <h1>Культуры</h1>
</div>

<%= render 'cultures_grid' %>

<%= link_to t('.new', :default => t("helpers.links.new")),
            new_culture_path,
            :class => 'btn btn-primary' %>

<button class="wg-external-csv-export-button btn btn-primary" data-grid-name="cultures">Export To CSV</button>
