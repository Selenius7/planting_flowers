<div class="page-header">
  <h1>Клиенты</h1>
</div>

<%= render 'clients_grid' %>

<%= link_to t('.new', :default => t("helpers.links.new")),
            new_client_path,
            :class => 'btn btn-primary' %>

<button class="wg-external-csv-export-button btn btn-primary" data-grid-name="clients">Export To CSV</button>
