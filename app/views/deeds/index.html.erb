<div class="page-header">
  <h1>Операции</h1>
</div>
<%= render 'deeds_grid' %>
<%= link_to t('.new', :default => t("helpers.links.new")),
            new_deed_path,
            :class => 'btn btn-primary' %>
